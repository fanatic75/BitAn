{
  "name": "bitan-server",
  "version": "1.0.0",
  "description": "server for bitchat",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && npm run debug",
    "build": "rm -rvf build && tsc",
    "prod": "cd ./build && node server.js",
    "debug": "cd ./build && node --inspect server.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0",
    "uniqid": "^5.0.3"
  },
  "author": "amaan75@github",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.12.12",
    "@types/socket.io": "^2.1.0",
    "@types/uniqid": "^4.1.2"
  }
}